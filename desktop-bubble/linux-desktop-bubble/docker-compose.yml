services:
  desktop-bubble:
    build: .
    image: desktop-bubble:latest
    network_mode: host
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:ro
    restart: unless-stopped

# Icons

Place your application icons here before building:

- `icon.ico` — Windows multi-size icon (required for Windows bundle)
- `icon.png` — 256×256 PNG fallback

You can generate these from the browser-bubble PNG icons or use a tool like
`cargo tauri icon <path-to-1024x1024.png>` once the Tauri CLI is installed.

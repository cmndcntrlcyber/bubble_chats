# Copy to .env — never commit .env to git
# These are read at runtime via std::env; set them in your shell or a .env loader.

# Required
ANTHROPIC_API_KEY=sk-ant-...

# Optional — enables live web search context via Tavily
TAVILY_API_KEY=tvly-...
